extends ../layout

block content
  h1=title
  form(method="POST") 
    p
      label(for='title') Title
      input#title(name='title' type='text' value=book.title)
    p
      label(for='author') Author
      input#author(name='author' type='text' value=book.author)
    p
      label(for='genre') Genre
      input#genre(name='genre' type='text' value=book.genre)
    p
      label(for='year') Year
      input#year(name='year' type='text' value=book.year)
    p
      input(type='submit' value='Update Book' formaction="/books/" + book.id) 
      //- input(type='submit' value='Update Book' formaction="/books/" + book.id + "/update") 

    p
      a.button(href='/books') Cancel
    p
      input(type='submit' value='Delete Book' formaction="/books/" + book.id + "/delete")

